# Server-side fallback key for the AI coach backend (server/).
# Used only when the user hasn't set their own key in the UI.
# No VITE_ prefix → never exposed to the client bundle. Do NOT commit real keys.
NVIDIA_API_KEY=nvapi-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
