// Stub for @enjoys/react-api's optional dynamic import of `dexie-observable`.
// Its useObservable() handler is a no-op, so an empty module is safe and avoids
// bundling a legacy Dexie addon.
export default {};
